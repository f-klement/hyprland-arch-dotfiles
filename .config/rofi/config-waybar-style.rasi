/* Waybar style switcher (WaybarStyles.sh) -- 2-column grid. */

configuration {
    modi: "drun,run";
}

@import "pywal-color/pywal-theme.rasi"
@import "theme/base.rasi"

window {
    width: 800px;
}

listview {
    columns:       2;
    lines:         8;
    fixed-columns: true;
}
