/* Waybar layout switcher (WaybarLayout.sh) -- 2-column grid. */

configuration {
    modi: "drun,run";
}

@import "pywal-color/pywal-theme.rasi"
@import "theme/base.rasi"

window {
    width: 480px;
}

listview {
    columns:       1;
    lines:         20;
    fixed-columns: true;
}
