/* Clipboard history picker (ClipManager.sh, SUPER+ALT+V) -- taller single-column list. */

configuration {
    modi: "drun,run";
}

@import "pywal-color/pywal-theme.rasi"
@import "theme/base.rasi"

window {
    width: 420px;
}

listview {
    lines:         20;
    fixed-columns: true;
}
