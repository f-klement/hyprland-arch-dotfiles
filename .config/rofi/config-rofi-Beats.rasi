/* Online radio/music picker (RofiBeats.sh, SUPER+SHIFT+M). */

configuration {
    modi: "drun,run";
}

@import "pywal-color/pywal-theme.rasi"
@import "theme/base.rasi"

window {
    width:  500px;
    height: 400px;
}

listview {
    lines:         7;
    fixed-columns: true;
}
