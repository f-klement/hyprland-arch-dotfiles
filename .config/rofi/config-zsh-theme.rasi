/* zsh theme switcher (submitted by github.com/hyprhex) -- 3-column grid. */

configuration {
    modi: "drun,run";
}

@import "pywal-color/pywal-theme.rasi"
@import "theme/base.rasi"

window {
    width: 800px;
}

listview {
    columns: 3;
    lines:   5;
}
