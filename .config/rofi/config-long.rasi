/* Emoji picker (RofiEmoji.sh, SUPER+ALT+E) -- tall single-column list. */

configuration {
    modi: "drun,run";
}

@import "pywal-color/pywal-theme.rasi"
@import "theme/base.rasi"

window {
    width: 700px;
}

listview {
    lines:         9;
    fixed-columns: true;
}
