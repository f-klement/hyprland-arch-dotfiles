# .ssh is no longer managed by this repo/stow -- it lives directly as
# plain files at ~/.ssh. Ignore it outright so it can never be re-added
# by accident (e.g. a stray `git add .` or `stow .`).
.ssh/
